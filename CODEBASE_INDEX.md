# Codebase Index

> Generated by Claude during onboarding (`codebase-onboard` skill).
> Updated after major structural changes.
> Both agents load this instead of re-scanning the codebase.
> GitNexus supplements this with live graph queries.

---

## Stack

- **Language**: <!-- e.g. TypeScript, Python -->
- **Framework**: <!-- e.g. Next.js 14, FastAPI -->
- **Database**: <!-- e.g. PostgreSQL via Prisma -->
- **Package manager**: <!-- e.g. npm, pnpm -->
- **Runtime**: <!-- e.g. Node 20, Python 3.11 -->

---

## How to Run

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Run tests
npm test

# Build for production
npm run build
```

---

## Directory Structure

```
/
├── src/                 # Main source
│   ├── app/             # Next.js app router (or pages/)
│   ├── components/      # UI components
│   ├── lib/             # Shared utilities
│   ├── services/        # Business logic
│   └── types/           # TypeScript types
├── prisma/              # DB schema + migrations
├── tests/               # Test files
├── public/              # Static assets
└── ...
```

---

## Key Files

| File | Purpose |
|------|---------|
| `src/lib/auth.ts` | Authentication logic |
| `src/services/` | Core business logic |
| `prisma/schema.prisma` | Database schema |
| `src/app/api/` | API route handlers |

---

## Entry Points

| Entry | Path |
|-------|------|
| Web app | `src/app/page.tsx` |
| API root | `src/app/api/` |
| Main server | `src/server.ts` |
| Background jobs | `src/jobs/` |

---

## Data Models

| Model | Description | Key relationships |
|-------|------------|------------------|
| `User` | App user | has many Orders |
| | | |

---

## API Routes

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/health` | Health check |
| POST | `/api/auth/login` | Login |
| | | |

---

## Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `DATABASE_URL` | Yes | Postgres connection string |
| `JWT_SECRET` | Yes | Auth token signing key |
| | | |

---

## Test Setup

- **Runner**: <!-- e.g. Vitest, Jest, Pytest -->
- **Test files location**: `tests/` or `*.test.ts`
- **Run tests**: `npm test`
- **Coverage**: `npm run test:coverage`

---

## Known Issues

<!-- Things Claude should know before touching code -->

-

---

## Dependency Graph (GitNexus)

<!-- Updated after running `gitnexus analyze` -->

- Graph indexed: <!-- date -->
- MCP tools available: `query()`, `context()`, `impact()`, `detect_changes()`
- To re-index: `gitnexus analyze --force`

---

_Last updated by Claude: <!-- date -->_
