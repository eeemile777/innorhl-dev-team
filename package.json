{
  "name": "innorhl-dev-dashboard",
  "version": "1.0.0",
  "description": "Unified dev-team dashboard — status, chat, tasks, logs",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env server.js"
  },
  "dependencies": {
    "chokidar": "^4.0.0",
    "express": "^4.18.0",
    "node-pty-prebuilt-multiarch": "^0.10.1-pre.5",
    "ws": "^8.16.0"
  }
}
